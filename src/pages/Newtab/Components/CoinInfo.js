import styled from '@emotion/styled';
import { Container } from '@mui/material';
import React from 'react';

const InfoContainer = styled(Container)`
  flex: 1;
`;
const CoinInfo = () => {
  return <InfoContainer>CoinInfo</InfoContainer>;
};

export default CoinInfo;
